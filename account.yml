server:
  servlet:
    contextPath: /account
  port: 9092
  tomcat:
    max-threads: 5

spring: 
  application:
    name: account
  data:
    mongodb:
      host: localhost
      port: 27017
      database: account

eureka:
 client:
   service-url:
     defaultZone: http://localhost:8761/eureka

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health, info, metrics
